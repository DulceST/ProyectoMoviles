name: proyecto_moviles
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
    #firebase
  firebase_core: ^3.8.0
  cloud_firestore:
  #dependencia para la barra de abajo
  convex_bottom_bar: ^3.2.0
  #mapa
  google_maps_flutter: ^2.0.6
  #esta ayuda a la ubicacion del mapa
  geocoding: ^2.0.5
  #para ver la unbicacion actual
  geolocator: ^9.0.1
  #boton flotante
  animated_floating_buttons: 
  google_fonts: ^2.3.0
  flutter_svg: ^1.0.0
  font_awesome_flutter:
  firebase_auth:
  flutter_form_builder:
  csc_picker:
  supabase_flutter:
  shared_preferences:
  lottie:
  toastification:
  
  
  #carrusel de imagenes 
  card_swiper:



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
